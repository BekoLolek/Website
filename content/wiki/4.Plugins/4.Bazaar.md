---
title: Bazaar
description: Guide on how the Bazzar works
---

# Bazaar

- You can open the menu using the command "/bazaar menu"
- There, the market changes depending on the amount of buyers and sellers of every product

<template>
  <div class="crafting-recipe">
    <p v-if="title" class="recipe-title">{{ title }}</p>
    <div class="crafting-container">
      <div class="crafting-grid">
        <div v-for="(item, i) in grid" :key="i" class="slot">
          <template v-if="item">
            <img :src="item.image" :alt="item.name" :title="item.name" draggable="false" />
            <span v-if="item.count && item.count > 1" class="count">{{ item.count }}</span>
          </template>
        </div>
      </div>
      <div class="arrow">▶</div>
      <div class="slot output-slot">
        <template v-if="output">
          <img :src="output.image" :alt="output.name" :title="output.name" draggable="false" />
          <span v-if="output.count && output.count > 1" class="count">{{ output.count }}</span>
        </template>
      </div>
    </div>
  </div>
</template>

<script setup>
defineProps({
  title: {
    type: String,
    default: null,
  },
  // 9-element array (3x3 grid, left-to-right, top-to-bottom).
  // Each element is null (empty) or { name, image, count? }.
  grid: {
    type: Array,
    default: () => Array(9).fill(null),
  },
  output: {
    type: Object,
    default: null,
  },
})
</script>

<style scoped>
.crafting-recipe {
  display: inline-flex;
  flex-direction: column;
  align-items: center;
  gap: 10px;
}

.recipe-title {
  margin: 0;
  color: #fff;
  font-family: 'Minecraft', monospace, sans-serif;
  font-size: 1rem;
  text-shadow: 2px 2px #3f3f3f;
  letter-spacing: 0.5px;
}

.crafting-container {
  display: flex;
  align-items: center;
  gap: 14px;
  background: #c6c6c6;
  padding: 12px;
  border: 3px solid;
  border-color: #fff #555 #555 #fff;
  box-shadow: inset -1px -1px 0 #000, inset 1px 1px 0 #fff;
}

.crafting-grid {
  display: grid;
  grid-template-columns: repeat(3, 40px);
  grid-template-rows: repeat(3, 40px);
  gap: 2px;
  background: #8b8b8b;
  padding: 4px;
  border: 2px solid;
  border-color: #373737 #c8c8c8 #c8c8c8 #373737;
}

.slot {
  width: 40px;
  height: 40px;
  background: #8b8b8b;
  border: 2px solid;
  border-color: #373737 #c8c8c8 #c8c8c8 #373737;
  display: flex;
  align-items: center;
  justify-content: center;
  position: relative;
  box-sizing: border-box;
  transition: background 0.1s;
}

.slot:hover {
  background: #a0a0e0;
}

.slot img {
  width: 32px;
  height: 32px;
  image-rendering: pixelated;
  object-fit: contain;
  pointer-events: none;
}

.count {
  position: absolute;
  bottom: 1px;
  right: 2px;
  font-size: 10px;
  font-weight: bold;
  color: #fff;
  text-shadow: 1px 1px #3f3f3f;
  line-height: 1;
  pointer-events: none;
}

.arrow {
  font-size: 22px;
  color: #555;
  user-select: none;
}

.output-slot {
  width: 52px;
  height: 52px;
  border-color: #373737 #c8c8c8 #c8c8c8 #373737;
}

.output-slot img {
  width: 40px;
  height: 40px;
}
</style>
